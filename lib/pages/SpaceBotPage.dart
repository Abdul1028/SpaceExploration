import 'package:flutter/material.dart';

class SpaceBotPage extends StatelessWidget {
  const SpaceBotPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'SpaceBot Page',
        style: TextStyle(fontSize: 24, color: Colors.white),
      ),
    );
  }
}
